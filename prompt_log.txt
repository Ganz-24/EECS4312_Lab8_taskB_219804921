## Student Name:
## Student ID: 

Prompt #1: {
    
I am working on an EECS 4312 lab for an Event Registration with Waitlist system. Before generating any code, I want you to review the specification below and identify any ambiguities, underspecified behaviors, contradictions, or missing clarifications. Do not generate code yet.

Problem Summary:
The system manages registration for a single event with a fixed capacity. It registers users until the event is full, places additional users into a first-in-first-out waitlist, and automatically promotes the earliest waitlisted user when a registered user cancels. It must prevent duplicate registrations, allow users to query their current status, preserve deterministic behavior under identical operation sequences, and keep system state consistent after every operation.

Functional Requirements:
1. The system shall accept a fixed capacity value at initialization, and capacity must remain constant for the event lifecycle.
2. The system shall register a user directly when registered_count is strictly less than capacity.
3. The system shall place a user into the waitlist when registered_count is equal to capacity.
4. The system shall preserve FIFO ordering in the waitlist for all enqueue and dequeue operations.
5. The system shall reject duplicate registration attempts for any user already present in registered or waitlist state by raising DuplicateRequest.
6. The system shall remove a registered user on cancellation and shall promote exactly one earliest waitlisted user if waitlist_count is greater than 0 and capacity allows.
7. The system shall remove a waitlisted user on cancellation and shall recompute subsequent waitlist positions so that positions remain contiguous starting at 1.
8. The system shall return user status as registered, waitlisted with exact 1-based position, or none within one query operation.
9. The system shall ensure registered_count never exceeds capacity after every operation.
10. The system shall support capacity equal to 0, where all successful registrations are waitlisted and registered_count remains 0.
11. The system shall maintain deterministic outcomes so that the same operation order always yields the same registered list and waitlist list.
12. The system shall provide ordered state output containing registered list and waitlist list after operations through snapshot functionality.

Constraints and Assumptions:
C1. Capacity must be an integer and must be greater than or equal to 0.
C2. The system manages exactly one event instance per EventRegistration object.
C3. Registered user count must never exceed capacity at any time.
C4. Waitlist ordering must follow FIFO with no reordering exceptions.
C5. User identifiers are treated as unique keys across all states.
C6. A user may exist in at most one state at a time.
C7. Waitlist positions must be 1-based and contiguous after each state change.
C8. Cancellation of an unknown user is expected to raise NotFound.
C9. Operations are processed sequentially, and deterministic behavior is required.
C10. Snapshot output must preserve deterministic ordering for validation.

Invariants:
1. A user shall not appear more than once in the complete system state.
2. A user shall not be simultaneously registered and waitlisted.
3. Registered_count shall always be less than or equal to capacity.

Negative Requirements:
1. The system shall not allow duplicate registration for a user already registered or waitlisted.
2. The system shall not produce a registered_count greater than capacity under any operation sequence.

Tie-Breaking Rules:
1. Promotion priority shall be earliest waitlisted user first, strictly FIFO.
2. If multiple valid outputs exist, the system shall choose the deterministic output defined by arrival order of registered and waitlist lists.
3. On duplicate register conflict, rejection via DuplicateRequest shall take priority over any attempt to reinsert or reorder the user.

Exceptions and Special Rules:
1. DuplicateRequest shall be raised for duplicate register attempts.
2. NotFound shall be raised when cancel is requested for a user absent from both registered and waitlist states.
3. Capacity = 0 is a mandatory edge case where all registrations are waitlisted and no promotion can create registered users.
4. Multiple consecutive cancellations shall preserve invariants after each cancellation, not only at end state.
5. Waitlisted user cancellation before promotion shall remove that user and shift later waitlist positions deterministically.

Acceptance Criteria:
AC1:
Given an event with capacity = 2 and no users
When users u1 and u2 are registered in order
Then both users must be registered in order [u1, u2], waitlist must be empty, and registered count must equal 2
Linked Constraint ID(s): C1, C2, C9

AC2:
Given an event with capacity = 2 and registered users [u1, u2]
When user u3 is registered
Then u3 must be waitlisted at position 1 and registered users must remain [u1, u2]
Linked Constraint ID(s): C2, C3, C7

AC3:
Given an event with capacity = 1, registered [u1], waitlist [u2, u3]
When user u1 cancels
Then u2 must be promoted to registered, u3 must become waitlist position 1, and u1 status must be none
Linked Constraint ID(s): C2, C3, C4, C7, C9

AC4:
Given an event with capacity = 1 and registered [u1]
When user u1 attempts to register again
Then the system must raise DuplicateRequest and state must remain unchanged
Linked Constraint ID(s): C5, C6

AC5:
Given an event with capacity = 1, registered [u1], waitlist [u2, u3]
When waitlisted user u2 cancels
Then waitlist must become [u3] and u3 must have waitlist position 1
Linked Constraint ID(s): C3, C7, C8

AC6:
Given an event with capacity = 0
When users u1 and u2 register
Then both users must be waitlisted in order [u1, u2], registered must remain empty, and no operation may place a user in registered
Linked Constraint ID(s): C1, C2, C3, C9

AC7:
Given a valid state where user ux does not exist
When cancel is called with ux
Then the system must raise NotFound
Linked Constraint ID(s): C10

AC8:
Given any valid system state
When status is queried for a registered user, waitlisted user, and absent user
Then results must be registered, waitlisted with correct 1-based position, and none respectively
Linked Constraint ID(s): C7

Expected API / Skeleton Area:
I will insert the provided code skeletons below. Please use them later when implementation begins and do not invent a different public API unless you explicitly identify a mismatch with the 

## Student Name:
## Student ID:

"""
Task B: Event Registration with Waitlist (Stub)
In this lab, you will design and implement an Event Registration with Waitlist system using an LLM assistant as your primary programming collaborator. 
You are asked to implement a Python module that manages registration for a single event with a fixed capacity. 
The system must:
•	Accept a fixed capacity.
•	Register users until capacity is reached.
•	Place additional users into a FIFO waitlist.
•	Automatically promote the earliest waitlisted user when a registered user cancels.
•	Prevent duplicate registrations.
•	Allow users to query their current status.

The system must ensure that:
•	The number of registered users never exceeds capacity.
•	Waitlist ordering preserves FIFO behavior.
•	Promotions occur deterministically under identical operation sequences.

The module must preserve the following invariants:
•	A user may not appear more than once in the system.
•	A user may not simultaneously exist in multiple states.
•	The system state must remain consistent after every operation.

The system must correctly handle non-trivial scenarios such as:
•	Multiple cancellations in sequence.
•	Users attempting to re-register after canceling.
•	Waitlisted users canceling before promotion.
•	Capacity equal to zero.
•	Simultaneous or rapid consecutive operations.
•	Queries during state transitions.

The output consists of the updated registration state and ordered lists of registered and waitlisted users after each operation.
"""

from dataclasses import dataclass
from typing import List, Optional


class DuplicateRequest(Exception):
    """Raised if a user tries to register but is already registered or waitlisted."""
    pass


class NotFound(Exception):
    """Raised if a user cannot be found for cancellation (if required by handout)."""
    pass


@dataclass(frozen=True)
class UserStatus:
    """
    state:
      - "registered"
      - "waitlisted"
      - "none"
    position: 1-based waitlist position if waitlisted; otherwise None
    """
    state: str
    position: Optional[int] = None


class EventRegistration:
    """
    Students must implement this class per the lab handout.
    Deterministic ordering is required (e.g., FIFO waitlist, predictable registration order).
    """

    def __init__(self, capacity: int) -> None:
        """
        Args:
            capacity: maximum number of registered users (>= 0)
        """
        # TODO: Initialize internal data structures
        raise NotImplementedError("EventRegistration.__init__ not implemented yet")

    def register(self, user_id: str) -> UserStatus:
        """
        Register a user:
          - if capacity available -> registered
          - else -> waitlisted (FIFO)

        Raises:
            DuplicateRequest if user already exists (registered or waitlisted)
        """
        # TODO: Implement per lab handout
        raise NotImplementedError("register not implemented yet")

    def cancel(self, user_id: str) -> None:
        """
        Cancel a user:
          - if registered -> remove and promote earliest waitlisted user (if any)
          - if waitlisted -> remove from waitlist
          - behavior when user not found depends on handout (raise NotFound or ignore)

        Raises:
            NotFound (if required by handout)
        """
        # TODO: Implement per lab handout
        raise NotImplementedError("cancel not implemented yet")

    def status(self, user_id: str) -> UserStatus:
        """
        Return status of a user:
          - registered
          - waitlisted with position (1-based)
          - none
        """
        # TODO: Implement per lab handout
        raise NotImplementedError("status not implemented yet")

    def snapshot(self) -> dict:
        """
        (Optional helper for debugging/tests)
        Return a deterministic snapshot of internal state.
        """
        # TODO: Implement if required/allowed
        raise NotImplementedError("snapshot not implemented yet")]

[import pytest

from solution import EventRegistration, UserStatus, DuplicateRequest, NotFound


def test_register_until_capacity_then_waitlist_fifo_positions():
    er = EventRegistration(capacity=2)

    s1 = er.register("u1")
    s2 = er.register("u2")
    s3 = er.register("u3")
    s4 = er.register("u4")

    assert s1 == UserStatus("registered")
    assert s2 == UserStatus("registered")
    assert s3 == UserStatus("waitlisted", 1)
    assert s4 == UserStatus("waitlisted", 2)

    snap = er.snapshot()
    assert snap["registered"] == ["u1", "u2"]
    assert snap["waitlist"] == ["u3", "u4"]


def test_cancel_registered_promotes_earliest_waitlisted_fifo():
    er = EventRegistration(capacity=1)
    er.register("u1")
    er.register("u2")  # waitlist
    er.register("u3")  # waitlist

    er.cancel("u1")  # should promote u2

    assert er.status("u1") == UserStatus("none")
    assert er.status("u2") == UserStatus("registered")
    assert er.status("u3") == UserStatus("waitlisted", 1)

    snap = er.snapshot()
    assert snap["registered"] == ["u2"]
    assert snap["waitlist"] == ["u3"]


def test_duplicate_register_raises_for_registered_and_waitlisted():
    er = EventRegistration(capacity=1)
    er.register("u1")
    with pytest.raises(DuplicateRequest):
        er.register("u1")

    er.register("u2")  # waitlisted
    with pytest.raises(DuplicateRequest):
        er.register("u2")


def test_waitlisted_cancel_removes_and_updates_positions():
    er = EventRegistration(capacity=1)
    er.register("u1")
    er.register("u2")  # waitlist pos1
    er.register("u3")  # waitlist pos2

    er.cancel("u2")    # remove from waitlist

    assert er.status("u2") == UserStatus("none")
    assert er.status("u3") == UserStatus("waitlisted", 1)

    snap = er.snapshot()
    assert snap["registered"] == ["u1"]
    assert snap["waitlist"] == ["u3"]


def test_capacity_zero_all_waitlisted_and_promotion_never_happens():
    er = EventRegistration(capacity=0)
    assert er.register("u1") == UserStatus("waitlisted", 1)
    assert er.register("u2") == UserStatus("waitlisted", 2)

    # No one can ever be registered when capacity=0
    assert er.status("u1") == UserStatus("waitlisted", 1)
    assert er.status("u2") == UserStatus("waitlisted", 2)
    assert er.snapshot()["registered"] == []

    # Cancel unknown should raise NotFound
    with pytest.raises(NotFound):
        er.cancel("missing")



#################################################################################
# Add your own additional tests here to cover more cases and edge cases as needed.
#################################################################################]

Your task right now:
1. Review the specification only.
2. List all ambiguities, missing clarifications, hidden assumptions, or possible contradictions.
3. For each issue, explain why it matters for implementation or testing.
4. If the requirements are already sufficient, say so explicitly.
5. Do not generate code yet.
}
Response #1: {

    The LLM response said that my specification is already pretty strong overall and covers most of the important parts of the system, especially FIFO waitlisting, deterministic behavior, exceptions, and the capacity = 0 case. It pointed out a few areas that could still be clarified, such as whether user IDs are case-sensitive, how registered users should be ordered, the exact format of the snapshot output, what should happen if capacity is negative, and making it clearer that promotions happen immediately after cancellations. It also mentioned clarifying that operations are sequential and not actually concurrent.

My decision is to modify the specification. I would not reject the response because most of its suggestions are helpful and make the requirements more precise. At the same time, I would not fully accept every point as equally important, because some of them seem more minor or theoretical. I would mainly update the specification to clearly define non-empty case-sensitive user IDs, preserve insertion order in the registered list, lock in the exact snapshot structure, and state that negative capacity should raise an error.
}


Prompt #2: {
    Ok it is time to make the code. Below is the documentation of what to do on the lab: OBJECTIVE OF THIS DOCUMENT In this lab, you will explore how requirements function when software implementation is delegated to a large language model (LLM). Modern development increasingly involves describing system behavior in natural language and relying on AI tools to generate code. In such workflows, the quality of the output depends heavily on how well the problem is specified before implementation begins. In this lab, you will complete a development task using an LLM assistant. Vibe coding means you describe intent in natural language and iteratively collaborate with an AI system that generates the implementation. The quality of the final system depends less on manual coding and more on: • How precisely you describe the problem • How clearly you define constraints • How early you anticipate edge cases • How strategically your write prompts WHAT YOU WILL PRACTICE By the end of this lab, you should be able to: • Write a structured requirements document • Define acceptance criteria and edge cases • Then use an LLM to implement the system • Log your prompts and iterations QUESTIONS For any questions, please attend the labs. BACKGROUND • Week 1 – Week 8 of lectures and labs. WHAT MATTERS IN THIS LAB This lab is not about clever algorithms. It is about observing what happens when your requirements meet an AI system. As you work, pay attention to: • How often you revise your requirements • Where ambiguities appear during implementation • When missing constraints surface • Whether debugging traces back to unclear specifications You must complete a structured requirements document before generating any code. Only after finalizing the specification form on eclass, you may use the LLM for implementation. The central question is simple: How does the clarity and completeness of your requirements shape the behavior of an AI-generated solution? Your goal is not to write complex algorithms. Your goal is to see how well your specification controls the system when execution is delegated. If something is ambiguous, document your assumption and proceed. VIBE CODING AND SOFTWARE REQUIREMENTS In this lab, you will design and implement an Event Registration with Waitlist system using an LLM assistant as your primary programming collaborator. You are asked to implement a Python module that manages registration for a single event with a fixed capacity. The system must: • Accept a fixed capacity. • Register users until capacity is reached. • Place additional users into a FIFO waitlist. • Automatically promote the earliest waitlisted user when a registered user cancels. • Prevent duplicate registrations. • Allow users to query their current status. The system must ensure that: • The number of registered users never exceeds capacity. • Waitlist ordering preserves FIFO behavior. • Promotions occur deterministically under identical operation sequences. The module must preserve the following invariants: • A user may not appear more than once in the system. • A user may not simultaneously exist in multiple states. • The system state must remain consistent after every operation. The system must correctly handle non-trivial scenarios such as: • Multiple cancellations in sequence. • Users attempting to re-register after canceling. • Waitlisted users canceling before promotion. • Capacity equal to zero. • Simultaneous or rapid consecutive operations. • Queries during state transitions. The output consists of the updated registration state and ordered lists of registered and waitlisted users after each operation. TODO TASK1: Requirements Development Begin by filling an online questionnaire “Lab8 Persona - Form1 Constraint Specification” on eclass. Note: Requirements must be testable, precise, and free of vague language. Avoid words such as “appropriate,” “reasonable,” or “efficient” unless you define them explicitly. During this phase, you should not consult the LLM for solution ideas. If ambiguity arises, document your assumption clearly and proceed. TASK 2- Initiating your repository: To begin your implementation, you must set up your GitHub repository correctly. Navigate to https://github.com/EECS4312TeachingTeam/EECS4312_Lab8_taskB . Fork this repository into your own GitHub account. Once the repository has been forked, add EECS4312TeachingTeam as a collaborator so the teaching team can access your work. You must complete all development on your own branch. Do not commit directly to the main branch. Ensure that your commits clearly show your development progress. You will submit this repository as part of your final lab deliverable. TASK 3 - LLM Interaction and Implementation: After your requirements are complete, you may begin interacting with the LLM. • Your first prompt must provide: • The problem summary, • The functional requirements, • The constraints, • The acceptance criteria. Before requesting code generation, ask the model whether any ambiguities or missing clarifications exist. Log this interaction carefully. All prompts must be recorded in prompt_log.txt, including their purpose, the full prompt text, a short summary of the response, and your decision to accept, modify, or reject it. As you implement the task, commit your changes to the repository. Your implementation must be contained in the file solution.py, and this file must reflect the new requirements introduced in this lab. During implementation, pay attention to whether you discover missing constraints, unclear requirements, or underspecified behavior. If you revise your requirements, the revision must be explicitly documented. TASK 4 - Testing and Validation: You must implement at least five executable tests. At least two of these must address edge cases you identified earlier. Tests should directly correspond to your acceptance criteria. This alignment is critical. If a test fails, determine whether the failure stems from: • An implementation issue, or • A missing or unclear requirement. This distinction will be important in your reflection. You should submit the tests.py including your test cases. TASK4 – Reflection: After implementation and test, please take a reflection and fill the questionnaire “Lab8 Persona - Form2 Prediction” on eclass, focus on the following questions: • Whether you revised your requirements after coding began, • Where ambiguity became visible, • Whether failures were due to specification gaps or coding issues, • How you structured your prompts, • What you would improve if given additional time. DELIVERABLES & SUBMISSION INSTRUCTIONS 1- Forms (on eClass) Before and after implementation, you must complete the following which should show your thought process properly: • Lab8 Persona – Form1 Constraint Specification (Must be completed before implementation begins) • Lab8 Persona – Form2 Prediction (Must be completed after implementation and testing) These forms are mandatory and part of your lab submission. 2- GitHub Repository Submission Your repository must have been forked from the main class repository and contain: • solution.py (Your implementation reflecting the updated requirements) • tests.py (New test cases validating the updated behavior) • prompt_log.txt (A file containing the full transcript of all LLM prompts and responses used during this lab) o Do NOT upload screenshots. Copy and paste the exact prompts and responses o The transcript must reflect the complete interaction history. INCOMPLETE SUBMISSIONS WHICH DO NOT INCLUDE ALL THE ABOVE COMPONENTS WILL NOT BE GRADED. Please adhere to academic honesty. This lab should be done individually. Your code, tests, specification, and documentation must be your own. We will be using additional test cases to test your implementation so make sure your test cases cover all the requirements of the system. I attatched the code in my last message. fill in the Todo areas and create the test cases needed. When you resond give me the full files
}
Response #2: {

Below are the two complete files you need for the lab:

solution.py (implementation with all TODOs filled)

tests.py (tests including at least five, with edge cases)

The implementation strictly follows the specification you provided:

FIFO waitlist

deterministic ordering

capacity invariant

promotion on cancel

duplicate detection

correct waitlist position updates

capacity = 0 behavior

snapshot format expected by the provided tests

solution.py
## Student Name:
## Student ID:

"""
Task B: Event Registration with Waitlist

Implementation based on the requirements defined in the lab specification.

Key properties:
- Fixed capacity per event
- FIFO waitlist
- Deterministic ordering
- Automatic promotion when registered user cancels
- Duplicate registration prevention
- Accurate waitlist positions
"""

from dataclasses import dataclass
from typing import List, Optional


class DuplicateRequest(Exception):
    """Raised if a user tries to register but is already registered or waitlisted."""
    pass


class NotFound(Exception):
    """Raised if a user cannot be found for cancellation."""
    pass


@dataclass(frozen=True)
class UserStatus:
    """
    state:
      - "registered"
      - "waitlisted"
      - "none"
    position: 1-based waitlist position if waitlisted; otherwise None
    """
    state: str
    position: Optional[int] = None


class EventRegistration:
    """
    Event registration system with deterministic ordering.
    """

    def __init__(self, capacity: int) -> None:
        """
        Args:
            capacity: maximum number of registered users (>= 0)
        """
        if capacity < 0:
            raise ValueError("capacity must be >= 0")

        self.capacity = capacity
        self.registered: List[str] = []
        self.waitlist: List[str] = []

    def register(self, user_id: str) -> UserStatus:
        """
        Register a user:
          - if capacity available -> registered
          - else -> waitlisted (FIFO)

        Raises:
            DuplicateRequest if user already exists
        """

        if user_id in self.registered or user_id in self.waitlist:
            raise DuplicateRequest()

        # Capacity zero means nobody can be registered
        if self.capacity == 0 or len(self.registered) >= self.capacity:
            self.waitlist.append(user_id)
            return UserStatus("waitlisted", len(self.waitlist))

        self.registered.append(user_id)
        return UserStatus("registered")

    def cancel(self, user_id: str) -> None:
        """
        Cancel a user:
          - if registered -> remove and promote earliest waitlisted user
          - if waitlisted -> remove from waitlist
          - if absent -> raise NotFound
        """

        # Registered cancellation
        if user_id in self.registered:
            self.registered.remove(user_id)

            # Promote earliest waitlisted user if capacity allows
            if self.waitlist and len(self.registered) < self.capacity:
                promoted = self.waitlist.pop(0)
                self.registered.append(promoted)

            return

        # Waitlisted cancellation
        if user_id in self.waitlist:
            self.waitlist.remove(user_id)
            return

        raise NotFound()

    def status(self, user_id: str) -> UserStatus:
        """
        Return status of a user:
          - registered
          - waitlisted with position
          - none
        """

        if user_id in self.registered:
            return UserStatus("registered")

        if user_id in self.waitlist:
            position = self.waitlist.index(user_id) + 1
            return UserStatus("waitlisted", position)

        return UserStatus("none")

    def snapshot(self) -> dict:
        """
        Return deterministic snapshot of internal state.
        """

        return {
            "registered": list(self.registered),
            "waitlist": list(self.waitlist),
        }
tests.py
import pytest

from solution import EventRegistration, UserStatus, DuplicateRequest, NotFound


def test_register_until_capacity_then_waitlist_fifo_positions():
    er = EventRegistration(capacity=2)

    s1 = er.register("u1")
    s2 = er.register("u2")
    s3 = er.register("u3")
    s4 = er.register("u4")

    assert s1 == UserStatus("registered")
    assert s2 == UserStatus("registered")
    assert s3 == UserStatus("waitlisted", 1)
    assert s4 == UserStatus("waitlisted", 2)

    snap = er.snapshot()
    assert snap["registered"] == ["u1", "u2"]
    assert snap["waitlist"] == ["u3", "u4"]


def test_cancel_registered_promotes_earliest_waitlisted_fifo():
    er = EventRegistration(capacity=1)

    er.register("u1")
    er.register("u2")
    er.register("u3")

    er.cancel("u1")

    assert er.status("u1") == UserStatus("none")
    assert er.status("u2") == UserStatus("registered")
    assert er.status("u3") == UserStatus("waitlisted", 1)

    snap = er.snapshot()
    assert snap["registered"] == ["u2"]
    assert snap["waitlist"] == ["u3"]


def test_duplicate_register_raises_for_registered_and_waitlisted():
    er = EventRegistration(capacity=1)

    er.register("u1")

    with pytest.raises(DuplicateRequest):
        er.register("u1")

    er.register("u2")

    with pytest.raises(DuplicateRequest):
        er.register("u2")


def test_waitlisted_cancel_removes_and_updates_positions():
    er = EventRegistration(capacity=1)

    er.register("u1")
    er.register("u2")
    er.register("u3")

    er.cancel("u2")

    assert er.status("u2") == UserStatus("none")
    assert er.status("u3") == UserStatus("waitlisted", 1)

    snap = er.snapshot()
    assert snap["registered"] == ["u1"]
    assert snap["waitlist"] == ["u3"]


def test_capacity_zero_all_waitlisted_and_promotion_never_happens():
    er = EventRegistration(capacity=0)

    assert er.register("u1") == UserStatus("waitlisted", 1)
    assert er.register("u2") == UserStatus("waitlisted", 2)

    assert er.status("u1") == UserStatus("waitlisted", 1)
    assert er.status("u2") == UserStatus("waitlisted", 2)

    assert er.snapshot()["registered"] == []

    with pytest.raises(NotFound):
        er.cancel("missing")


# -----------------------
# Additional Required Tests
# -----------------------

def test_reregister_after_cancel():
    """Edge case: user cancels then registers again."""
    er = EventRegistration(capacity=1)

    er.register("u1")
    er.cancel("u1")

    status = er.register("u1")

    assert status == UserStatus("registered")
    assert er.snapshot()["registered"] == ["u1"]


def test_multiple_cancellations_with_multiple_promotions():
    """Edge case: sequential cancellations causing multiple promotions."""
    er = EventRegistration(capacity=2)

    er.register("u1")
    er.register("u2")
    er.register("u3")
    er.register("u4")

    er.cancel("u1")
    er.cancel("u2")

    snap = er.snapshot()

    assert snap["registered"] == ["u3", "u4"]
    assert snap["waitlist"] == []


def test_status_for_absent_user():
    """Edge case: querying status of unknown user."""
    er = EventRegistration(capacity=2)

    er.register("u1")

    assert er.status("unknown") == UserStatus("none")


def test_waitlist_position_after_multiple_changes():
    """Edge case: ensure waitlist positions remain contiguous."""
    er = EventRegistration(capacity=1)

    er.register("u1")
    er.register("u2")
    er.register("u3")
    er.register("u4")

    er.cancel("u3")

    assert er.status("u2") == UserStatus("waitlisted", 1)
    assert er.status("u4") == UserStatus("waitlisted", 2)


def test_constructor_negative_capacity():
    """Edge case: invalid capacity."""
    with pytest.raises(ValueError):
        EventRegistration(-1)

}
--------------------------------------------------